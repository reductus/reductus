Reflbin is a tool for integrating NCNR 2-D ICP files.

Requires libz and a C compiler in order to run.

To build, modify Makefile to use the correct compiler flags and options,
then type:

    make

For instructions on running the program, run:

    ./reflbin
